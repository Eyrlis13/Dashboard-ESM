# === Données nominatives — NE JAMAIS versionner (règle de sécurité ESM) ===
# On garde le dossier bilans/ (avec sa notice) mais on ignore tout son contenu.
bilans/*
!bilans/README.md
*.xlsx
*.xls

# Saisies manuelles de ressentis (référencées par nom de dossier -> non publiées)
ressentis_manuel.csv

# Le dataset anonymisé peut être publié, mais on ignore les sorties locales de test
/tmp_dataset*.json

# Divers
__pycache__/
*.pyc
.DS_Store
.venv/
venv/

# Classification curée des aides techniques (référencée par nom de dossier)
at_curated.csv
